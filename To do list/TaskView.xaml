<UserControl x:Class="To_do_list.TaskView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:To_do_list"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             x:Name="root">
    <Grid>
        <StackPanel Margin="5" Orientation="Horizontal">
            <CheckBox x:Name="Checkbox"  VerticalAlignment="Center" Command="{Binding ToggleCompletionCommand, ElementName=root}" 
                      CommandParameter="{Binding Description}"
                      Checked="ToggleStrikethrough" Unchecked="ToggleStrikethrough"/>
            <TextBlock x:Name="DescriptionBlock" FontSize="18" Text="{Binding Description}" VerticalAlignment="Center"/>
        </StackPanel>
    </Grid>
</UserControl>
