<Window x:Class="To_do_list.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:To_do_list"
        xmlns:uc="clr-namespace:To_do_list"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="300">
    <Grid>
        <StackPanel>
            <ListBox Grid.Column="0" ItemsSource="{Binding Tasks}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <uc:TaskView Description = "{Binding Description}"/>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
            <TextBlock>
                <TextBlock.InputBindings>
                    <MouseBinding Command="{Binding CreateNewTask}" MouseAction="LeftClick"/>
                </TextBlock.InputBindings>
                Добавить задачу
            </TextBlock>
        </StackPanel>
    </Grid>
</Window>
